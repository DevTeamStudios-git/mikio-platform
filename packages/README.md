# packages

Shared libraries used across apps and backend (e.g. `ui`, `sdk`, `common`, `logger`). Code duplicated across two or more layers belongs here, not copy-pasted — see `DTS_ENGINEERING_HANDBOOK.md` §5.
