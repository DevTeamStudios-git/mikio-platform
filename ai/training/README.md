# training

Training pipeline: continued pretraining, instruction tuning, preference optimization. See `ARCHITECTURE.md` §6 and `TECH_STACK.md` §8 for the stack (PyTorch, Transformers, PEFT, DeepSpeed, Axolotl).
