# evaluation

Benchmark suite and evaluation pipeline (HumanEval, MBPP, MMLU, SWE-Bench, LiveCodeBench). A model release requires a completed, documented evaluation pass here — see `DTS_ENGINEERING_HANDBOOK.md` §9.
