# models

DTS's own trained model releases, under the `str-*` naming convention (e.g. `str-3.5-pro/`, `str-4/`). `str-*` names model-family artifacts specifically — Mikio is the product, STR is the model family. See `DTS_ENGINEERING_HANDBOOK.md` §3.
