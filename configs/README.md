# configs

Environment and service configuration files.

- `postgresql.md` — local PostgreSQL + pgvector provisioning (direct install, no Docker) and `DATABASE_URL` setup
- `.env.example` — template for local environment variables (copy to `.env`; real values are gitignored)
